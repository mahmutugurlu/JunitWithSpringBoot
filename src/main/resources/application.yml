spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/junit_db
    username: techpro
    password: password
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL9Dialect
        format_sql: true
